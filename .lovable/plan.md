

## Ajuste padding y tamaño de letra en opciones de evaluación

### Cambio en `src/pages/estudiante/EvaluacionPage.tsx`

**Línea 37**: Cambiar `p-4` (16px) a `p-3` (12px) en el `<label>` del `SelectableOption`.

**Línea 54**: Cambiar `text-sm` a `text-base` en el `<span>` del texto de la opción para mejorar legibilidad.

